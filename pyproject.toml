[project]
name = "gatedsrp"
version = "0.1.0"
description = "Reproducibility code for Gated Spatial Redundancy Projection."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
